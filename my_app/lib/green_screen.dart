import 'package:flutter/material.dart';

class GoodJobScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    /*
    return Container(
      color: Colors.green,
    );
    */
    return Scaffold(
      appBar: AppBar(
        title: Text('Test'),
      ),
      body: Container(
        color: Color.fromARGB(255, 140, 212, 142),
        ),
      );
  }
}